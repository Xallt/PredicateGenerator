[project]
name = "predicategenerator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.4.3",
    "tyro>=1.0.8",
]

[dependency-groups]
dev = [
    "pyright>=1.1.408",
    "ruff>=0.15.5",
]
